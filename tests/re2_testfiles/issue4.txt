issue #4 Modified
=================

    >>> import re2
    >>> TERM_SPEC2 = re2.compile('([\W\d_]*)(([^\W\d_]*[-\.]*)*[^\W\d_])([\W\d_]*[^\W\d_]*)')
    >>> TERM_SPEC2.search("a").groups()
    ('', 'a', None, '')


Still broken because of unicode:
    >>> TERM_SPEC2.search("Hello").groups()
    ('', 'Hello', 'Hell', '')
